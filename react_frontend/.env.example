# For local development, leave empty (Vite proxy handles /api → localhost:8000)
# For production, set to the deployed Django backend URL:
# VITE_API_URL=https://eventhub-django.onrender.com/api
VITE_API_URL=
